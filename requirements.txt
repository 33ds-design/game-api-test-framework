fastapi>=0.100.0
uvicorn>=0.20.0
requests>=2.28.0
pytest>=8.0.0
pytest-html>=4.0.0
pytest-json-report>=0.5.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
httpx>=0.27.0
# generate_screenshots.py 依赖 Pillow 渲染报告截图
Pillow>=10.0.0
# httpcore 在每次请求里执行 `import sniffio`（见 httpcore/_synchronization.py）。
# 缺这个包时导入失败不会被 sys.modules 缓存，于是每次请求都重扫一遍 sys.path，
# 压测吞吐会腰斩。装上它只是让该导入命中缓存，属于性能必需项。
sniffio>=1.3.1